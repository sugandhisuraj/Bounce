import {Platform, StyleSheet} from 'react-native';

import {getHp, getWp, FONTSIZE} from '../../../app/utils';
export default StyleSheet.create({
  
  bWfContainerStyle: {width: '90%', marginVertical: getHp(20)},
  searchContainer: {
    width: '80%', 
  }
});
